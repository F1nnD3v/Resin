const fileManager = () => {

    return (
        <div className="bg-black text-white mr-4 pt-2 pl-4 pr-4 h-full mt-auto max-h-screen min-h-screen">
            <p>{"{Project_name}"}</p>
            <div className={"flex bg-gray-400 h-0.5 m-0 mt-1 w-full"} />
        </div>
    );
}

export default fileManager;
